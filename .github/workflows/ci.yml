name: CI- Build 

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/steup-node@v3
        with: 
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: build project
        run: npm run build  
